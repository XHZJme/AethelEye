# 安全策略

## 报告安全问题

请不要在公开 Issue、讨论区或 Pull Request 中披露未修复漏洞、有效密钥、Cookie、数据库、个人信息或可直接利用的生产数据。

请发送邮件至 [xhzj.me@qq.com](mailto:xhzj.me@qq.com)，至少提供：

- 受影响版本或提交；
- 问题类型与影响范围；
- 使用虚构/脱敏数据的复现步骤；
- 你建议的修复方向（如有）。

不要发送真实账号凭据，也不要在未获授权的系统上验证漏洞。

## 安全默认值

- Web 服务默认只监听 `127.0.0.1`；
- CORS 默认只接受本机前端地址；
- JWT 密钥首次启动时在本地随机生成；
- 账号、Cookie、代理密码、Webhook 地址/签名密钥、MCP 鉴权请求头与 LLM API Key 使用本地密钥加密保存；Webhook 与 LLM 配置接口只返回“已配置”状态，不回显秘密值；
- 日志写入前统一遮蔽常见秘密字段和 URL 查询参数，并且不记录 LLM/Webhook 的错误响应正文；
- 运行数据、密钥、日志、截图、录屏与构建产物默认不进入 Git；
- 开源版不包含商业许可证服务器或自动上传日志/诊断报告的链路；
- 开源版不包含浏览器指纹混淆逻辑。

插件与 Skill 执行仅有尽力而为的 Python 级限制，并非操作系统级隔离。上传、安装、审核并启用插件或 Skill，等同于授权第三方代码在 AethelEye 服务进程中运行。安装接口受管理员权限保护，ZIP 包同时限制路径、文件数和体积，但这些措施不能证明第三方代码安全；只应使用来源可信、版本固定且已经人工审计的内容。高风险环境应在独立低权限账户、容器或虚拟机中运行。

如需把服务开放到局域网或公网，请在此之前增加 TLS、反向代理、来源限制、强认证、系统防火墙、访问日志审计和备份加密，并重新进行威胁建模。

## 密钥泄露响应

如果秘密值曾经进入 Git 暂存区、提交、发布包或截图：

1. 立即在对应服务端撤销或轮换；
2. 停止继续发布受影响版本；
3. 清理文件与 Git 历史；
4. 重新运行密钥和个人信息扫描；
5. 按组织制度评估通知、取证和监管义务。
