# 隐私与数据流说明

AethelEye 是本地优先的软件，但“本地运行”不等于“绝不联网”。是否产生外部通信取决于使用者启用的功能和填写的配置。本文件说明开源版的默认边界，不能替代你所在组织的隐私评估、数据分级或法律审查。

## 默认本地数据

运行时数据保存在仓库根目录的 `data/`，包括数据库、JWT 密钥、日志、截图、录屏、浏览器配置、Cookie、代理凭据、Webhook 配置、MCP 鉴权请求头、LLM API Key、插件与 AI 记忆。Webhook 地址/签名密钥、MCP 鉴权请求头和 LLM API Key 以由本地随机密钥派生的 Fernet 密钥进行认证加密；Webhook 与 LLM 配置接口只返回“已配置”状态，不回显秘密值。这不能替代操作系统访问控制。`data/` 及常见数据库、日志、密钥和构建文件已被 `.gitignore` 排除。

开源仓库不包含遥测地址、作者服务器地址、商业许可证服务器、自动日志上传或自动诊断报告上传功能。

## 可能的外部接收方

| 功能 | 何时联网 | 可能发送的数据 | 建议 |
| --- | --- | --- | --- |
| 页面采集 | 创建并运行任务时 | URL、登录态、HTTP 请求、页面交互 | 仅访问已授权页面，限制频率，不绕过安全措施 |
| 企业微信告警 | 配置并触发 Webhook 时 | 告警标题、价格、链接、备注等 | 使用专用机器人，避免个人信息和商业秘密 |
| LLM 服务 | 配置服务商并主动调用时 | 提示词、上下文、所选业务数据 | 先脱敏，确认服务商协议、保存政策和跨境要求 |
| GitHub 下载 | 主动安装远程 Skill 时 | 请求的仓库地址及网络元数据 | 只安装可信来源，固定版本并检查代码 |

## 操作建议

1. 使用虚构数据做开发、演示和截图。
2. 为每个环境使用独立账号、Webhook 和 API Key，遵循最小权限原则。
3. 不在 URL、任务名、备注或日志中写入密码、Cookie、Token 或身份证件信息。
4. 限制 `data/` 目录的系统访问权限，并按业务需要设置最短保留期。
5. 对外分享前同时扫描工作区、Git 暂存区和完整 Git 历史。
6. 一旦密钥可能泄露，应先在服务端撤销/轮换；仅从 Git 删除不能使旧密钥失效。
7. 公开 Issue、截图和录屏中不得包含真实账号、浏览器资料、内网地址、客户信息或运营数据。

## 联系方式

隐私或安全问题请发邮件至 [xhzj.me@qq.com](mailto:xhzj.me@qq.com)，并避免在邮件主题或正文中直接粘贴有效秘密值。
